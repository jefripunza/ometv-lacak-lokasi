{
  "tasks": {
    "dev": "deno run --watch=./server.ts --allow-net server.ts",
    "start": "chmod +x ./server && ./server",
    "compile": "rm -rf server server.exe && deno compile -A --output server server.ts"
  }
}
